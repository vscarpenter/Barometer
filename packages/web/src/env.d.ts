// Build-time constants substituted by Vite's `define` (see vite.config.ts).
// They are plain literals in the emitted bundle — not runtime variables — so
// they're typed as ambient consts rather than added to a global interface.
declare const __APP_VERSION__: string;
declare const __BUILD_TIME__: string;
